<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Items</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <%- include('navbar') %>
    <% items.map((item) => { %>
    <a href='/items/details/<%=item.id%>'><div class="overview"><%= item.name %></div></a>
    <% }) %>
    <a href="/items/create"><button>Create</button></a>
</body>
</html>